# Secrets — never commit
.env
.env.*
!.env.example
*.key
*.pem
*.p12
*.pfx
secrets/
vault/

# Uploaded originals — local only, read-only
/sources

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.coverage
htmlcov/
dist/
build/
*.egg-info/
.venv/
venv/
env/

# OS
.DS_Store
Thumbs.db
ehthumbs.db

# Claude Code local session state (never commit)
.claude/settings.local.json

# IDE / editors
.vscode/
.idea/
*.swp
*.swo

# Logs
*.log
logs/

# Runtime bridge data (conversation history, events -- never commit)
memory/log.jsonl
memory/chats/

# Autonomy-guard runtime state -- never commit (control flag + per-run audit stream)
memory/SAFE_MODE
memory/agent-runs.jsonl
memory/runner-state.json

# Owner-office workspace -- Shelly personal-task data; never commit (A3 hardening 2026-06-12)
memory/owner-office/

# Security scan staging area
temp-repo-scan/

# Node (for any future integrations)
node_modules/

# Obsidian -- vault rooted at repo root; keep app.json + plugin configs, ignore UI state/cache
.obsidian/workspace.json
.obsidian/workspace-mobile.json
.obsidian/cache
.obsidian/*.log
# Legacy wiki-scoped vault state (if used); app.json + plugin configs stay tracked
memory/wiki/.obsidian/workspace.json
memory/wiki/.obsidian/workspace-mobile.json
memory/wiki/.obsidian/cache
memory/wiki/.obsidian/*.log


# Obsidian clippings + unsorted personal scratch -- local only, never commit
/Clippings/
/To Sort/

# Coverage output -- generated by jest/vitest; never commit (Rambo SEC clearance 2026-07-02)
projects/ai-patient-simulator/app/apps/api/coverage/
projects/ai-patient-simulator/app/packages/engine-test-harness/coverage/

# Obsidian plugin bundles -- minified vendor JS; not project code (Rambo SEC clearance 2026-07-02)
memory/wiki/.obsidian/plugins/
