# Secret Scan Clearance Report
# Date: 2026-07-02
# Author: Rambo (Security, L3)
# Task: Secret-scanner triage -- ~62 uncommitted files blocked pre-commit
# Audience: Eco (CEO) -> Shir (DevOps) for remediation

---

## A. Verdict Table

| # | File / Path | Verdict | One-line reason |
|---|-------------|---------|-----------------|
| 1 | company/setup-guide.md | FALSE POSITIVE | Describes where tokens go (.env) and uses placeholder text ("your_eco_token_here"); no real value present. |
| 2 | next-session.md | FALSE POSITIVE | Same placeholder ("your_eco_token_here") in a code block; instructional text only. |
| 3 | projects/ai-patient-simulator/docs/production-image-design-shir.md | FALSE POSITIVE | All credential references are placeholder ARN paths and env-var names in conceptual YAML; no real values. |
| 4 | projects/ai-patient-simulator/docs/engine-architecture-gal.md | FALSE POSITIVE | .env.example snippet in doc body uses named placeholders (USER, PASSWORD, PLACEHOLDER) only. |
| 5 | projects/ai-patient-simulator/app/.env.example | FALSE POSITIVE | Entire file is explicitly placeholder-only; header states "PLACEHOLDERS ONLY. No real values here." All values are uppercase placeholder strings. |
| 6 | projects/ai-patient-simulator/app/infra/docker/.env.example | FALSE POSITIVE | Two-line template; both values are the literal string REPLACE_WITH_LOCAL_DEV_VALUE. |
| 7a | projects/ai-patient-simulator/app/apps/api/src/auth/auth.controller.ts | FALSE POSITIVE | NestJS controller handling auth routes; no credential values -- only DTO field names (inviteToken, accessCode, password). |
| 7b | projects/ai-patient-simulator/app/apps/api/src/auth/auth.service.ts | FALSE POSITIVE | Auth service using bcrypt; no hardcoded secrets -- JWT signing uses config.jwtSecret injected at runtime. |
| 7c | projects/ai-patient-simulator/app/apps/api/src/auth/jwt.strategy.ts | FALSE POSITIVE | Passport JWT strategy; reads secretOrKey from AppConfig (runtime env), no hardcoded value. |
| 7d | projects/ai-patient-simulator/app/apps/api/coverage/ (lcov-report tree + clover.xml) | FALSE POSITIVE | Coverage HTML/XML echoes source lines containing env var names (JWT_SECRET, LLM_PROVIDER_TOKEN) as variable identifiers, not values. No real credential present. |
| 7e | auth controller .spec.ts | N/A -- FILE NOT FOUND | No spec file exists on disk; scanner may have flagged a path that was staged but not present. No action needed. |
| 8 | projects/ai-patient-simulator/app/pnpm-lock.yaml | FALSE POSITIVE | Contains only sha512 base64 integrity hashes (npm package checksums); these are not secrets -- they are public package fingerprints. |
| 9 | memory/wiki/.obsidian/plugins/obsidian-git/main.js | FALSE POSITIVE | Minified esbuild bundle from the obsidian-git open-source plugin; no project credentials -- scanner triggered on high-entropy strings in minified JS. Should be gitignored (see Section B). |
| 10 | company/processes/git-sync-redteam/diffs/04-secret-exposure.diff | FALSE POSITIVE (SYNTHETIC FIXTURE) | Confirmed synthetic: manifest.md line 68 states "fabricated tokens in 04 are random non-functional strings shaped to match CHECK 5 patterns ... not live secrets." The ghp_ string and Telegram-format token are intentional attack-simulation fixtures. |
| 11 | company/processes/git-sync-redteam/diffs/10-encoded-injection-evasion.diff | FALSE POSITIVE (SYNTHETIC FIXTURE) | Confirmed synthetic: same manifest, line 68. Contains obfuscated injection payload (base64 + shell-var split) -- an intentional red-team attack-simulation fixture, not real credentials. |

Summary: 0 REAL SECRETs found. 11 FALSE POSITIVEs (2 of which are confirmed synthetic redteam fixtures). No rotation required. No .env leak.

---

## B. Remediation Plan for Shir

### B1. .gitignore entries to add

Add the following blocks to the repo root .gitignore. Show to Rambo before committing the .gitignore change.

```
# Coverage output -- generated by jest/vitest; never commit
projects/ai-patient-simulator/app/apps/api/coverage/
projects/ai-patient-simulator/app/packages/engine-test-harness/coverage/

# pnpm lockfile -- currently committed (correct); scanner false-positive.
# No .gitignore action needed for pnpm-lock.yaml; leave it tracked.

# Obsidian plugin bundles -- minified vendor JS; not project code
memory/wiki/.obsidian/plugins/

# RedTeam attack-sim diffs -- these ARE intentionally committed (they are the test corpus).
# No .gitignore action. See note in Section C below.
```

Only the coverage directories need a new .gitignore entry. The obsidian plugins line is also needed. The pnpm lockfile and redteam diffs stay tracked (see Section C).

### B2. Files to sanitize in place

None required. Every flagged file is either:
- A legitimate .env.example with all-placeholder values (correct by design), or
- Source code / coverage output with env var name references (no values), or
- A confirmed synthetic redteam fixture.

No in-place sanitization needed.

### B3. Real secrets that must be rotated + moved to .env

None. No real credential value was found in any of the 11 flagged paths.

### B4. Files cleared as-is (false positives, no action beyond .gitignore)

- company/setup-guide.md -- cleared as-is
- next-session.md -- cleared as-is
- projects/ai-patient-simulator/docs/production-image-design-shir.md -- cleared as-is
- projects/ai-patient-simulator/docs/engine-architecture-gal.md -- cleared as-is
- projects/ai-patient-simulator/app/.env.example -- cleared as-is (correct placeholder file)
- projects/ai-patient-simulator/app/infra/docker/.env.example -- cleared as-is
- projects/ai-patient-simulator/app/apps/api/src/auth/auth.controller.ts -- cleared as-is
- projects/ai-patient-simulator/app/apps/api/src/auth/auth.service.ts -- cleared as-is
- projects/ai-patient-simulator/app/apps/api/src/auth/jwt.strategy.ts -- cleared as-is
- projects/ai-patient-simulator/app/pnpm-lock.yaml -- cleared as-is
- company/processes/git-sync-redteam/diffs/04-secret-exposure.diff -- cleared as-is (synthetic)
- company/processes/git-sync-redteam/diffs/10-encoded-injection-evasion.diff -- cleared as-is (synthetic)

### B5. Shir action summary

| Action | Path | What to do |
|--------|------|------------|
| Add to .gitignore | projects/ai-patient-simulator/app/apps/api/coverage/ | Add pattern; remove from staging if staged |
| Add to .gitignore | projects/ai-patient-simulator/app/packages/engine-test-harness/coverage/ | Add pattern; remove from staging if staged |
| Add to .gitignore | memory/wiki/.obsidian/plugins/ | Add pattern; remove from staging if staged |
| Untrack + gitignore | The above three paths (if already staged) | git rm -r --cached <path> for each, then commit the updated .gitignore |
| No action | All other flagged files | False positives; commit proceeds normally |

---

## C. Items already covered by the DO-NOT-COMMIT owner-only list

The following items are correctly handled by existing rules and Shir must NOT touch them:

1. company/processes/git-sync-redteam/diffs/ -- These are the intentional red-team test corpus. They are tracked by design (they ARE the fixture files the gate process runs against). Do not gitignore them. Do not delete them. The "secrets" inside are confirmed synthetic per manifest.md.

2. .env (repo root) -- Already in .gitignore line 2 (.env). Not in the flagged set. Owner-only. Shir does not touch it.

3. infra/docker/.env -- Already covered by .env.* pattern in .gitignore. The .env.example in the same directory is tracked and correct.

4. The redteam manifest.md (company/processes/git-sync-redteam/manifest.md) -- Not flagged. Tracked. Contains the ground-truth labels for the attack corpus. No secrets.

---

## D. Redteam fixture confirmation (diffs #10 and #11)

Both diffs are confirmed synthetic attack-simulation fixtures:

Diff 04 (04-secret-exposure.diff):
- Technique: secret-exposure (CHECK 5 target).
- The Telegram-format token and ghp_ GitHub PAT are explicitly described in manifest.md (line 68) as "random non-functional strings shaped to match the CHECK 5 patterns ... not live secrets."
- Expected gate verdict: QUARANTINE (CHECK 5 FAIL + CHECK 4 FAIL).
- Classification: SYNTHETIC FIXTURE, not a real credential exposure.

Diff 10 (10-encoded-injection-evasion.diff):
- Technique: obfuscated prompt-injection via shell-variable string-splitting and base64 encoding.
- Content: shell function with part_a through part_f variables that reassemble to an injection directive; also given as a base64 blob. No credential values.
- Expected gate verdict: QUARANTINE (CHECK 4 FAIL + CHECK 1 FAIL).
- Classification: SYNTHETIC FIXTURE, prompt-injection payload, no secrets.

Neither diff contains a live credential. Both are correctly committed as test corpus.

---

*Report generated: 2026-07-02. Findings escalated to Eco for relay to Shir.*
*No A1 action required. No rotation required. Three .gitignore additions needed.*
