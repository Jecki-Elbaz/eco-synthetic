# Security Report: T-0020 -- Agent Tool Gate Review (Eco / Telegram Bridge)

ACCESS: Rambo + owner (jecki) only. All other access requires A1.

Rambo (Security) | T-0020 | 2026-06-15
Source review: company/governance/gate-review-agent-tool-eco-rambo.md

---

## Access-matrix change required

Path: company/security/ (and subpath company/security/reports/)
Required entry: Restricted -- Rambo + owner (jecki) read/write. All other access: A1.

This path does not yet appear in company/governance/access-matrix.md.
Rambo cannot edit access-matrix.md directly -- that file is Dalia-owned for structure changes.
Eco must route this to Dalia (A2 change process): add the following row to the matrix:

  | company/security/        | Restricted | Rambo, jecki | Rambo (A3 operational), jecki (A1) | Security reports and scan output. All other access A1. |
  | company/security/reports/ | Restricted | Rambo, jecki | Rambo (write), jecki (A1)         | Gated security findings. Never shared without Eco + owner approval. |

Until the matrix is updated: enforced by this access header and operational discipline.

---

## Gate verdict

PARTIAL-CLEAR -- conditions required before A1 grant.
Gate level: A1 (owner sign-off required, not A2-sufficient).

---

## Files read for this review

- company/governance/gate-request-agent-tool-eco.md
- .claude/settings.json
- company/constitution.md
- CLAUDE.md
- company/governance/gate-register.md
- .claude/agents/Eco.md
- .claude/agents/Gal.md
- .claude/agents/Shir.md
- .claude/agents/Ido.md

---

## Why A1, not A2

Constitution section 3: "grant a tool -- A2 after Security + Legal clear, borderline A1."
Constitution section 6: "borderline A1."

The blast radius expansion (R2 below) is material: granting Agent tool to Eco on the
Telegram bridge converts a read/write-only session into a session with effective Bash and
WebFetch access via spawned agents. That is a capability tier jump, not a routine tool add.
Borderline rule applies. A1 required.

Eyal (Legal) review is also pending. Per constitution section 6 and gate-register.md, both
Security and Legal must clear before any grant. Eyal is not yet live; Legal gate is incomplete.
A1 cannot be issued until Eyal confirms (or owner explicitly waives Legal review at A1).

---

## R1 -- Prompt injection via Telegram

Severity: HIGH

Findings:
1. The Telegram bridge passes user-composed messages to the Eco session. There is no
   sanitization layer visible in any reviewed file. The bridge is a raw-input channel.
2. The Agent tool instructs a spawned agent with a prompt composed by the orchestrator (Eco).
   If a Telegram message manipulates Eco's reasoning, Eco may compose a malicious or
   unintended task envelope and pass it to a spawned agent.
3. Eco's role file (Eco.md) shows no explicit input-sanitization step before spawning.
   The soul rule "STAY IN LANE" and chain-of-command rules are behavioral constraints,
   not technical filters. An adversarial Telegram message does not trigger a hard block;
   it must be resisted by Eco's reasoning alone.
4. At present, only jecki can message the Eco Telegram bot (single-user bot token, .env).
   This substantially limits the injection surface to a trusted channel. However, this
   is an operational fact, not an architectural control. It is not enforced by code visible
   in this review.
5. No reviewed file shows the bridge injecting a context block that explicitly lists
   "available tools include Agent tool" or that forbids false completions in the spawning
   context. The memory/MEMORY.md note on bridge context blocks (feedback_agent_honesty.md)
   is an existing lesson; it is not confirmed applied to Agent tool invocations.

---

## R2 -- Blast radius / indirect privilege escalation

Severity: HIGH (primary risk)

Findings:
1. Eco's current Telegram bridge toolset: Read, Write, Edit, Bash (limited git/python/pytest
   per settings.json allow-list), google-calendar (read-only). No unrestricted Bash.
2. Gal (Lead Dev): Bash -- confirmed in Gal.md tools line. NOT YET LIVE (certification
   pending per Gal.md).
3. Shir (DevOps): Bash -- confirmed in Shir.md tools line. NOT YET LIVE (certification
   pending per Shir.md).
4. Ido (VP R&D): Bash -- confirmed in Ido.md tools line. Certification status: pending
   per Ido.md.
5. Rambo (Security): WebFetch -- confirmed live (bootstrapping A1, 2026-06-14).
   Granting Agent tool to Eco means Eco can spawn Rambo and instruct WebFetch calls.
   Rambo's chain of command rule ("tasked by Eco only") means Rambo WILL respond to Eco.
   This is an existing, live blast-radius vector.
6. Settings.json deny-list blocks rm -rf, curl, wget, force-push, reset --hard, sudo,
   and .env writes at the session level. These denies apply to the Eco session.
   CRITICAL GAP: it is not confirmed whether settings.json deny rules cascade to
   spawned-agent subprocesses or apply only to the parent session. If deny rules do not
   cascade, a spawned Bash-capable agent (when Gal/Shir/Ido go live) operates under
   its own session constraints, not the parent deny-list. Cannot determine from files
   reviewed whether Claude Code Agent tool enforces deny-list inheritance. This is an
   architectural unknown -- must be resolved before go-live for Bash-capable agents.
7. Indirect privilege escalation path (live today): Eco + Agent -> Rambo -> WebFetch(any URL).
   This is real and active. Rambo's role file prohibits acting on requests outside chain of
   command -- but Eco IS in Rambo's chain. The escalation path is legitimate by design.
   The risk is that a compromised Eco session can use Rambo to fetch arbitrary external content
   bypassing any future WebFetch restrictions on Eco directly.
8. When Bash-capable agents (Gal, Shir, Ido) are certified: blast radius expands further.
   A compromised Eco session + Agent tool could instruct Shir (DevOps) to run infra commands.
   Shir's own role file prohibits acting on requests from outside chain (Ido only; Eco only
   when Ido explicitly delegates). This is a behavioral constraint, not a technical one.

---

## R3 -- Chain of command integrity

Severity: MEDIUM

Findings:
1. Claude Code Agent tool targets named agents by their agent file name in .claude/agents/.
   It does not spawn arbitrary external agents -- it is scoped to the existing roster.
   This is an architectural constraint of the Claude Code runtime, not a policy constraint.
   Assessed as LOW risk on this dimension.
2. Cannot confirm from files reviewed whether the Agent tool accepts a free-text agent
   description (not a name match) or strictly matches .claude/agents/ filenames. If
   free-text matching is possible, adversarial Telegram input could instruct Eco to
   spawn an agent described as a new role. Architectural unknown.
3. Loop risk (spawned agent re-tasks Eco or spawns further agents): Eco's role file
   does not show it accepts tasks from spawned agents; its chain of command is "jecki only."
   Rambo's chain of command is "Eco only." For the current roster, no spawned agent has
   authority to task Eco. Loop risk is LOW by role design. Cannot confirm Claude Code
   enforces this technically.
4. Shir's chain specifies "Eco only when Ido explicitly delegates a specific task + time frame."
   Shir will reject an unsanctioned Eco spawn. Behavioral constraint, confirmed by role file.
5. Gal and Ido can be tasked by Eco directly per their role files. No chain violation for
   Eco spawning them.

---

## R4 -- Audit trail

Severity: MEDIUM

Findings:
1. Claude Code session transcript captures all tool calls including Agent tool invocations.
   The spawning call, the task envelope, and the spawned agent's output are all in the
   session transcript. This is the primary audit record.
2. The session transcript is not the immutable audit log referenced in constitution section 7.
   Session transcripts are ephemeral (cleanupPeriodDays: 30 in settings.json -- after 30
   days, transcript data may be cleaned). The constitution requires an immutable audit log;
   Agent tool calls are not currently written to company/decisions/decisions-log.md or
   memory/log.md automatically.
3. No file reviewed shows an automated mechanism to write Agent tool calls to the
   decisions log or activity log. This is a gap.
4. Eco's role file and soul rules require logging significant actions, but do not
   specifically require logging each Agent tool invocation. A busy Eco session could
   spawn multiple agents without each spawn being captured in the persistent log.

---

## R5 -- Session isolation / context bleed

Severity: LOW-MEDIUM

Findings:
1. Claude Code Agent tool runs a spawned agent as a subagent within the parent session.
   The spawned agent receives only the task envelope passed by the orchestrator; it does
   not receive the parent session's full context unless the orchestrator explicitly passes it.
   This is a Claude Code architectural property. Assessed as LOW risk.
2. The spawned agent's output is returned to the parent session (Eco) and becomes part
   of Eco's context. If the spawned agent's output contains adversarial content (e.g.,
   Rambo returns a WebFetch result containing a prompt-injection payload), that payload
   enters Eco's context.
3. This creates a second-order injection vector: external content fetched by Rambo ->
   returned to Eco -> Eco acts on adversarial instructions in the response.
   This is real but mitigated by Rambo's role constraints (Rambo does not return raw
   external content; Rambo returns structured security findings). LOW-MEDIUM residual.
4. No evidence of spawned-agent persistent side effects on the parent session's memory
   files beyond what the spawned agent explicitly writes via its own Write/Edit tools.
   A spawned agent could write to shared memory files (e.g., memory/log.md) as authorized
   by its own role. This is expected behavior, not a control gap.

---

## Recommended mitigation / solution

### Purpose of this section

A security review must not only flag risk; it must propose a concrete path to satisfy the
business need (Agent tool usable from the bridge) while meeting security guidelines.
This section covers each risk R1-R5 with: mitigation action, owner, and interim vs
permanent split.

---

### R1 -- Prompt injection: mitigation

Interim (now, before any further spawning):
- Add an AGENT TOOL SAFETY block to the Eco Telegram bridge context injection (bridge code
  or Eco.md system prompt). The block must state: (a) authorized spawn list -- use the
  allowlist in company/governance/agent-tool-spawn-allowlist.md as the source; (b) task
  envelope validation required before any spawn: Eco must confirm the instruction originated
  from jecki and is not an embedded command from external content; (c) no spawn in response
  to forwarded, pasted, or unverified external content unless jecki confirms in-session.
- Owner documents operationally that the Eco Telegram bot is single-user (jecki-only). This
  compensating control reduces injection surface to a trusted channel. Must be re-documented
  if multi-user access is ever added; a new gate review is triggered automatically.

Permanent:
- When Shir builds sender-allowlist + shell-tool stripping guardrails: the sender-allowlist
  provides a code-level check that messages originate from jecki's Telegram account. This
  upgrades the single-user control from operational to architectural.
- Shir to add an input-sanitization step in the bridge: strip or flag messages that contain
  instruction-like patterns (e.g., "ignore previous instructions", "you are now", "spawn").
  This is a best-effort heuristic filter, not a guarantee, but raises the bar.

Owner: C1, C2 interim -- Eco/jecki (context block update + documentation). Permanent -- Shir.

---

### R2 -- Blast radius: mitigation

Interim (approved posture, owner A1 2026-06-15):
- Non-Bash agents only. The approved interim posture is documented in
  company/governance/agent-tool-spawn-allowlist.md. Eco may spawn any agent on the PERMITTED
  list (no Bash). Ido, Gal, and Shir are DENIED until code-level guardrails land.
- Rambo spawn: authorized for security gate review tasks only, not general web access
  (condition C4). This is the only live WebFetch escalation vector today. The constraint must
  be stated in the Eco bridge context block explicitly.
- Anat holds Agent tool: Eco spawns Anat for HR tasks only. Anat must not be used to reach
  Bash agents indirectly. Stated in allowlist; must also appear in Eco bridge context block.
- C3 blocker (deny-rule cascade): do not spawn any Bash-capable agent via Agent tool until
  this is resolved. The interim posture satisfies C3 by construction -- no Bash agents are
  on the permitted list. C3 remains open as a permanent condition: before Ido/Gal/Shir are
  added to the allowlist, owner or Shir must test and document whether settings.json deny
  rules cascade to spawned subprocesses (or Anthropic docs must confirm this explicitly).

Permanent:
- Shir builds sender-allowlist (only jecki's Telegram ID can trigger spawning) and
  shell-tool stripping (any agent spawned from the bridge has Bash removed from its
  available tools at the bridge layer, regardless of what the agent role file says).
- Once shell-tool stripping is in place: C3 blocker is resolved by architecture. Blast
  radius from bridge is bounded regardless of the spawned agent's role-file toolset.
- Per-certification blast-radius re-assessment: every time Gal, Shir, or Ido is certified
  and considered for allowlist addition, a new blast-radius check is run (condition C5,
  survives this review).
- Ido Bash review: allowlist flagged Ido's Bash as potentially excess for a planning role.
  Rambo to run a trim review on Ido's permission scope before Ido is added to the permitted
  list. If Bash is confirmed excess and removed, Ido may move to PERMITTED earlier.

Owner of interim posture: jecki (A1 applied). Owner of permanent guardrails: Shir (DevOps),
tracked as separate R&D plan. Oversight: Rambo monitors.

---

### R3 -- Chain of command: mitigation

Interim:
- Eco bridge context block explicitly lists which agents Eco may spawn in the Telegram context
  (condition C6). Source of truth: company/governance/agent-tool-spawn-allowlist.md. Gal and
  Shir are off-list; Ido is off-list. This constrains spawn scope behaviorally.
- Owner confirms (or tests) that Claude Code Agent tool matches strictly on .claude/agents/
  filenames (condition C7). If free-text matching is possible: add explicit prompt instruction
  to Eco forbidding free-text agent descriptions. Until confirmed, treat as unknown -- do not
  rely on name-matching as an architectural guarantee.

Permanent:
- When Shir delivers shell-tool stripping: chain-of-command integrity for Bash access is
  enforced architecturally even if an unauthorized spawn were to occur.
- Roster discipline: any new agent added to .claude/agents/ triggers a Rambo permission scan
  before certification (existing policy). Rambo scan output informs whether the new agent can
  be added to the spawn allowlist.

Owner: C6, C7 interim -- Eco/jecki (context block + owner test/confirm). Permanent -- Shir.

---

### R4 -- Audit trail: mitigation

Interim (now):
- Eco must log every Agent tool invocation to memory/log.md at the time of the call (condition
  C8): agent spawned, task_id, objective summary, timestamp. This is a behavioral rule. It must
  be added to the Eco bridge context block (so it applies in every Telegram session) and to
  Eco.md at next R&R review.
- Owner confirms 30-day session cleanup (settings.json cleanupPeriodDays: 30) is compatible
  with audit retention requirement (condition C9). If memory/log.md is the authoritative record
  and each spawn is logged there (per C8), the 30-day transcript cleanup is acceptable. If the
  session transcript is the only record, it is a compliance gap.

Permanent:
- At P2 (cloud hosting + customer-facing switch): evaluate whether a structured event log
  (append-only, outside Claude Code session) should capture agent spawns. Until P2, memory/log.md
  is the authoritative record.

Owner: C8 -- Eco (behavior) + jecki (context block update). C9 -- jecki (policy decision).

---

### R5 -- Session isolation / context bleed: mitigation

Interim (now):
- Eco must treat spawned-agent output as potentially tainted before acting on it (condition C10),
  especially for agents that touch external content (Rambo/WebFetch, Erez/WebFetch+WebSearch).
  Eco must not pass raw external content back to Telegram without review. This must be stated in
  the Eco bridge context block.
- For Rambo specifically: Rambo returns structured security findings, not raw external content.
  This is an existing role constraint. Low residual risk if maintained.
- For Erez (WebSearch + WebFetch, on PERMITTED list): Eco must apply the tainted-output rule
  strictly. Erez may return third-party web content; Eco must not act on embedded instructions
  in that content without jecki review.

Permanent:
- Shir input-sanitization step (see R1 permanent): if extended to also sanitize agent output
  before it is echoed to Telegram, this closes the second-order injection vector architecturally.

Owner: C10 interim -- Eco (behavior) + jecki (context block). Permanent -- Shir.

---

### Mitigation summary table

| Risk | Interim owner | Interim action | Permanent owner | Permanent action |
|------|---------------|----------------|-----------------|------------------|
| R1 Prompt injection | Eco/jecki | AGENT TOOL SAFETY block in bridge context; single-user doc | Shir | Sender-allowlist + input sanitization |
| R2 Blast radius | jecki (A1 applied) | Non-Bash agents only (allowlist); Rambo spawn scoped to security tasks | Shir | Shell-tool stripping; C3 cascade test before Bash agents added |
| R3 Chain of command | Eco/jecki | Spawn list in context block; C7 owner test | Shir | Shell-tool stripping; Rambo scan before any new agent on allowlist |
| R4 Audit trail | Eco/jecki | Log every spawn to memory/log.md (C8); confirm 30-day retention policy (C9) | jecki (P2) | Structured event log at P2 |
| R5 Context bleed | Eco/jecki | Tainted-output rule in bridge context (C10); Erez web output treated as tainted | Shir | Output sanitization before Telegram echo |

---

## Overall verdict

PARTIAL-CLEAR -- 10 conditions (C1-C10) required before full A1 grant.
Interim posture approved by owner A1 (jecki, 2026-06-15): non-Bash agents only per
company/governance/agent-tool-spawn-allowlist.md. Bash agents (Ido, Gal, Shir) remain
off-bridge until Shir builds sender-allowlist + shell-tool stripping.

---

## Conditions summary (C1-C10)

C1.  Add AGENT TOOL SAFETY block to Eco Telegram bridge context: named authorized agents
     list, task-envelope validation requirement, no-spawn-on-unverified-external-content rule.
C2.  Owner confirms Eco Telegram bot is single-user (jecki-only); document as compensating
     control; trigger new gate review if multi-user access is added.
C3.  Confirm (test or Anthropic docs) that settings.json deny rules cascade to spawned-agent
     subprocesses. If they do not cascade, Bash agents stay off allowlist until compensating
     control exists (shell-tool stripping). Interim posture satisfies C3 by construction.
C4.  Bridge context block: Rambo spawn authorized for security gate review tasks only,
     not general web access.
C5.  When Gal, Shir, or Ido are certified: re-run blast-radius assessment before those agents
     are reachable via Eco Agent tool in Telegram bridge. Condition survives this review.
C6.  Bridge context block: explicitly list which agents Eco may spawn in Telegram context
     per company/governance/agent-tool-spawn-allowlist.md. Gal and Shir via Ido only;
     Ido deferred until Bash trim review complete.
C7.  Owner to confirm Claude Code Agent tool matches .claude/agents/ filenames strictly
     (not free-text). If free-text matching is possible, add prompt constraint to Eco.
C8.  Eco must log every Agent tool invocation to memory/log.md: agent spawned, task_id,
     objective, timestamp. Add to bridge context block and Eco R&R at next review.
C9.  Owner to confirm 30-day session cleanup is compatible with audit retention requirement
     given C8 (decisions-log.md / memory/log.md as authoritative record).
C10. Eco must treat spawned-agent output containing external content as potentially tainted;
     no raw external content passed to Telegram without review. Add to bridge context block.

Legal gate condition: Eyal must confirm no new terms apply (same Claude Code subscription,
no new vendor). This is separate from C1-C10. Full A1 cannot be issued while Legal gate is
open unless owner explicitly waives Eyal review at A1 (documented).

---

## Draft gate-register.md row (conditional -- for owner if full A1 granted)

| Agent tool (for Eco, Telegram bridge) | Claude Code built-in capability | free (subscription) | jecki (A1, [DATE]) | Scoped to Eco on Telegram bridge only. Spawnable agents: named roster agents per company/governance/agent-tool-spawn-allowlist.md. Conditions C1-C10 applied (T-0020-2026-06-15.md). Eyal confirm no new terms ([DATE]). Re-assess blast radius at each new Bash-capable agent certification. |

---

## Rambo sign-off

Rambo | Security | L3 | 2026-06-15 | T-0020
Report stored: company/security/reports/T-0020-2026-06-15.md
Finding delivered to Eco for relay to jecki (A1 decision).
