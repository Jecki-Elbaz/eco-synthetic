{
  "framework": "nextjs",
  "installCommand": "pnpm install --frozen-lockfile",
  "buildCommand": "pnpm --filter @aps/db generate && pnpm --filter @aps/web build",
  "outputDirectory": "apps/web/.next",
  "regions": ["iad1"]
}
