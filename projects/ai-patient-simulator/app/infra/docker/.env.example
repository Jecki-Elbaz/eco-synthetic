# APS local-dev container credentials -- TEMPLATE.
# Copy this file to ".env" in this same directory (infra/docker/.env is gitignored)
# and set local-dev-only values. NEVER put real/production credentials here.
POSTGRES_PASSWORD=REPLACE_WITH_LOCAL_DEV_VALUE
MINIO_ROOT_PASSWORD=REPLACE_WITH_LOCAL_DEV_VALUE
